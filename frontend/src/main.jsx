import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App'; // Pastikan jalurnya benar
import './index.css'; // Opsional, jika memiliki file CSS

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
